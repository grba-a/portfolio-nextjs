/** Staklena pilula s točkom iznad naslova sekcije (element s predloška). */
export default function Eyebrow({ children }: { children: React.ReactNode }) {
  return <p className="pill-eyebrow">{children}</p>;
}
